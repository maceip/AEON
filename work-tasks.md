# Work Tasks — Subagent Queue

Pick up tasks in order. Mark `[x]` when done. Do NOT touch files outside the listed scope.
Run `npx tsc --noEmit` after each change to verify no type errors.

## Tasks

(none right now)
